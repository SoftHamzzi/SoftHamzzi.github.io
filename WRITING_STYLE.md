# WRITING_STYLE.md

`_posts/game_dev/{devlog,portfolio}` 글을 쓰거나 고칠 때 지키는 규칙. `EP_Main`(devlog 표지)과
`EP_Gameplay_Framework-1`을 파일럿으로 정리했다. 새 글도, 기존 글을 손볼 때도 이 문서를 먼저 본다.

## 왜 이 문서가 있는가

devlog 초안은 AI가 쓴 티가 났다. 코드가 너무 많이 그대로 노출되고, 문체가 뻣뻣하고,
한눈에 안 들어오고, 미래 시점의 지식을 과거 시점 글에 섞어 썼다. 감점 요소라고 판단해서
전면 재작업하기로 했고, 그 과정에서 나온 규칙을 여기 모아둔다.

## 두 카테고리의 성격이 다르다

| 카테고리 | 성격 | 규칙 |
|---|---|---|
| `Portfolio` | "지금 어떻게 되어 있나" 통합 문서 | 바뀌면 고친다 |
| `DevLog` | "그때 뭘 했나" 개발 기록 | 안 고친다. 계속 쌓인다 |

## 문장

- **본문은 한다체**("만들었다", "때문이다"), **표지 상단 📌 안내문만 합니다체**.
- **문단은 1~3문장.** 길게 이어 쓰지 않는다.
- **em-dash(—)는 어디에도 쓰지 않는다.**
- **middot(·)도 쓰지 않는다.** 링크를 나열할 땐 줄바꿈(행 끝에 공백 두 칸), 단어를 나열할 땐 쉼표.
- **"~인가"/"~것인가"로 끝내지 않는다.** 웬만하면 단어로 종결한다. 정 질문형이 필요하면 "~인지"를 쓴다.
  - 나쁜 예: "GameMode가 아는 것 중 무엇을 GameState로 올릴 것인가."
  - 좋은 예: "GameMode에 있는 것 중 무엇을 GameState로 올릴지" / "판단 기준은 하나, UI 표시 여부다."
- **극적인 "반전 선언"체를 쓰지 않는다.** "진짜 결정은 반대 방향이다" 같은 문장은 AI가 즐겨 쓰는 티가 난다.
  그냥 무엇이 결정 대상인지 평서문으로 말한다.
- **없는 근거를 과장해서 만들지 않는다.** "연타는 손가락을 혹사시킨다" 같은 거창한 이유는 실제
  판단 근거가 아니면 쓰지 않는다. 확실하지 않은 결정은 "이 판단은 나중에 바뀔 수 있다"처럼
  잠정적이라고 솔직하게 적는다.
- **장르 용어는 직역하지 않는다.** "추출 슈터"가 아니라 "익스트랙션 슈터"(커뮤니티가 실제로 쓰는 표현).
- 문제→원인→해결 같은 서사가 있는 문단은 **번호(1. 2. 3.)**로 쪼갠다. 긴 흐름형 문장으로 풀지 않는다.

## 제목

- 콜론(:) 제목 자체는 괜찮다. **콜론 뒤에 빈 약속을 넣지 않는다.**
  - 나쁜 예: "PlayerController가 어디에 존재하는지: 나중에 중요해진다", "이 글의 본론", "다음 편의 씨앗이 있다"
  - 좋은 예: "PlayerController는 서버에도 전원 분량 있다", "원인: 두 함수가 다른 프레임의 시계를 읽고 있었다"
  - 기준: 제목만 읽어도 결론이 뭔지 알 수 있어야 한다. 안 그러면 스킴이 안 된다.
- 소제목은 명사구로 간결하게. 문장형 요약(예: "이 뼈대 위에 다음 편들이 얹는 것")보다 "관련 편"처럼 단어로.

## 코드

- 코드 블록은 본문 흐름에 그대로 두지 않고 **`<details markdown="1"><summary>...(접기/펼치기)</summary>`로 접는다.**
  - kramdown이라 `<details>`에 `markdown="1"`이 없으면 안의 코드 펜스가 렌더링되지 않는다.
  - `<summary>` 끝에 항상 `(접기/펼치기)`를 붙인다.
- 본문에는 결론과 표만 남기고, 코드는 "확인하고 싶은 사람만 펼치는" 보조 자료로 둔다.

## 표

- 실제 비교/매핑 데이터(A vs B, 요구사항→결정)에만 쓴다.
- 목록 항목마다 똑같은 리듬의 "한 줄 후크"를 붙이는 표는 만들지 않는다 — 모든 행이
  "사실 + 위트있는 태그" 패턴으로 통일되면 그 자체가 AI 생성 티다.

## 다이어그램 · 이미지

- `mermaid: true`를 front matter에 넣고 구조/흐름은 **mermaid로 그린다.** 접지 않는다, 바로 보인다.
- 진짜 스크린샷/GIF가 필요한 자리(변경 전후 비교, 실제 플레이 장면 등)는 **글 쓰는 사람이 먼저
  추측하지 말고, 글을 쓰면서 발견하는 대로 사용자에게 구체적으로 짚어 알린다.** 지금 안 넣어도
  되는 자리에 억지로 이미지를 요구하지 않는다.

## DevLog는 미래를 모른다

DevLog는 "그때 뭘 했나"이므로, 그 글을 쓰던 시점 이후에 나온 devlog는 존재하지 않는 것처럼 쓴다.

- 같은 번호 계열의 바로 다음 편(예: 1-1 글에서 1-2/1-3/1-4)은 언급 가능 — 같은 작업을 하던 중
  이미 계획된 근접 미래이기 때문이다.
- 다른 챕터, 특히 훨씬 나중에 쓰인 챕터(예: 1챕터 글에서 2챕터나 4챕터의 구체적 결론)를
  인용하지 않는다. 그건 나중에서야 알게 된 것이다.
- 이 규칙은 Portfolio에는 적용되지 않는다. Portfolio는 "지금 상태"이므로 시스템 전체를
  자유롭게 넘나들며 인용해도 된다.

## Devlog 표지(`EP_Main`류) 구조

- 편 전체를 나열하는 표(연재 목록)는 두지 않는다. 개별 글 링크는
  `/categories/devlog`(날짜순 자동 목록)에 맡긴다.
- 표지는 프로젝트 소개(필수/우대요건 매핑) + 핵심 컨셉 정도로 가볍게 유지한다.
  검증 방법처럼 "그 이후에 나오는 내용"은 표지에 넣지 않는다 — 표지는 시작할 때의 의도를
  보여주는 자리다.

## 적용 순서

새 글을 손볼 땐 이 순서로 훑는다.

1. 제목 · excerpt: 직역/AI 티 확인
2. 안내문 블록: `·`, 링크 줄바꿈
3. 본문: 문단 길이, "~인가" 종결, 반전 선언체, em-dash
4. 소제목: 빈 약속 콜론 제거
5. 코드: `<details markdown="1">`로 접기, `(접기/펼치기)` 라벨
6. 표: 실제 비교 데이터인지 확인, 아니면 목록/문장으로
7. 시간성(devlog만): 미래 챕터 인용 확인
